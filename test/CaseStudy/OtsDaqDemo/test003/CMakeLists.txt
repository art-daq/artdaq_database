execute_process( COMMAND "date" "-u" "+%m%d%Y-%H%M%S" OUTPUT_VARIABLE CONFIG_NUMBER )
string( STRIP ${CONFIG_NUMBER} CONFIG_NUMBER )
set (TEST_CONFIGURATION_NAME "testConfigA${CONFIG_NUMBER}")


SET ( TEST_NAME "store" )



SET (TEST_COLLECTION_NAME "ConfigurationAliases" )
SET ( TEST_FILE "${TEST_COLLECTION_NAME}_v0_pretty.json")

SET ( TEST_NUMBER "001" )

SET ( TEST_DBPROVIDER "filesystem" )
string(RANDOM LENGTH 5 ALPHABET 0123456789 RAND_NAME)
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json @ONLY )
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json @ONLY )
# Register test with CTest
add_test(NAME OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t
         COMMAND ${CMAKE_BINARY_DIR}/bin/OtsDaqDemo_t -o ${TEST_NAME} -p ${TEST_DBPROVIDER} -c ${TEST_COLLECTION_NAME} -f json -v ${TEST_VERSION_NAME}_${TEST_NUMBER}
  -g ${TEST_CONFIGURATION_NAME} -e OTSDAQ
  -s ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json -x ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json
         WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR})
set_tests_properties(OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t PROPERTIES
    ENVIRONMENT "ARTDAQ_DATABASE_URI=${ARTDAQ_DATABASE_URI_OTS_FILESYSTEM}")


SET ( TEST_DBPROVIDER "mongo" )
string(RANDOM LENGTH 5 ALPHABET 0123456789 RAND_NAME)
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json @ONLY )
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json @ONLY )
# Register test with CTest
add_test(NAME OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t
         COMMAND ${CMAKE_BINARY_DIR}/bin/OtsDaqDemo_t -o ${TEST_NAME} -p ${TEST_DBPROVIDER} -c ${TEST_COLLECTION_NAME} -f json -v ${TEST_VERSION_NAME}_${TEST_NUMBER}
  -g ${TEST_CONFIGURATION_NAME} -e OTSDAQ
  -s ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json -x ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json
         WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR})
set_tests_properties(OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t PROPERTIES
    ENVIRONMENT "ARTDAQ_DATABASE_URI=${ARTDAQ_DATABASE_URI_OTS_MONGO}")





SET (TEST_COLLECTION_NAME "Configurations" )
SET ( TEST_FILE "${TEST_COLLECTION_NAME}_v0_pretty.json")

SET ( TEST_NUMBER "002" )

SET ( TEST_DBPROVIDER "filesystem" )
string(RANDOM LENGTH 5 ALPHABET 0123456789 RAND_NAME)
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json @ONLY )
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json @ONLY )
# Register test with CTest
add_test(NAME OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t
         COMMAND ${CMAKE_BINARY_DIR}/bin/OtsDaqDemo_t -o ${TEST_NAME} -p ${TEST_DBPROVIDER} -c ${TEST_COLLECTION_NAME} -f json -v ${TEST_VERSION_NAME}_${TEST_NUMBER}
  -g ${TEST_CONFIGURATION_NAME} -e OTSDAQ
  -s ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json -x ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json
         WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR})
set_tests_properties(OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t PROPERTIES
    ENVIRONMENT "ARTDAQ_DATABASE_URI=${ARTDAQ_DATABASE_URI_OTS_FILESYSTEM}")


SET ( TEST_DBPROVIDER "mongo" )
string(RANDOM LENGTH 5 ALPHABET 0123456789 RAND_NAME)
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json @ONLY )
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json @ONLY )
# Register test with CTest
add_test(NAME OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t
         COMMAND ${CMAKE_BINARY_DIR}/bin/OtsDaqDemo_t -o ${TEST_NAME} -p ${TEST_DBPROVIDER} -c ${TEST_COLLECTION_NAME} -f json -v ${TEST_VERSION_NAME}_${TEST_NUMBER}
  -g ${TEST_CONFIGURATION_NAME} -e OTSDAQ
  -s ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json -x ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json
         WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR})
set_tests_properties(OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t PROPERTIES
    ENVIRONMENT "ARTDAQ_DATABASE_URI=${ARTDAQ_DATABASE_URI_OTS_MONGO}")






SET (TEST_COLLECTION_NAME "DefaultConfigurations" )
SET ( TEST_FILE "${TEST_COLLECTION_NAME}_v0_pretty.json")

SET ( TEST_NUMBER "003" )

SET ( TEST_DBPROVIDER "filesystem" )
string(RANDOM LENGTH 5 ALPHABET 0123456789 RAND_NAME)
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json @ONLY )
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json @ONLY )
# Register test with CTest
add_test(NAME OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t
         COMMAND ${CMAKE_BINARY_DIR}/bin/OtsDaqDemo_t -o ${TEST_NAME} -p ${TEST_DBPROVIDER} -c ${TEST_COLLECTION_NAME} -f json -v ${TEST_VERSION_NAME}_${TEST_NUMBER}
  -g ${TEST_CONFIGURATION_NAME} -e OTSDAQ
  -s ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json -x ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json
         WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR})
set_tests_properties(OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t PROPERTIES
    ENVIRONMENT "ARTDAQ_DATABASE_URI=${ARTDAQ_DATABASE_URI_OTS_FILESYSTEM}")


SET ( TEST_DBPROVIDER "mongo" )
string(RANDOM LENGTH 5 ALPHABET 0123456789 RAND_NAME)
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json @ONLY )
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json @ONLY )
# Register test with CTest
add_test(NAME OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t
         COMMAND ${CMAKE_BINARY_DIR}/bin/OtsDaqDemo_t -o ${TEST_NAME} -p ${TEST_DBPROVIDER} -c ${TEST_COLLECTION_NAME} -f json -v ${TEST_VERSION_NAME}_${TEST_NUMBER}
  -g ${TEST_CONFIGURATION_NAME} -e OTSDAQ
  -s ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json -x ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json
         WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR})
set_tests_properties(OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t PROPERTIES
    ENVIRONMENT "ARTDAQ_DATABASE_URI=${ARTDAQ_DATABASE_URI_OTS_MONGO}")


SET (TEST_COLLECTION_NAME "DetectorConfiguration" )
SET ( TEST_FILE "${TEST_COLLECTION_NAME}_v0_pretty.json")

SET ( TEST_NUMBER "004" )

SET ( TEST_DBPROVIDER "filesystem" )
string(RANDOM LENGTH 5 ALPHABET 0123456789 RAND_NAME)
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json @ONLY )
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json @ONLY )
# Register test with CTest
add_test(NAME OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t
         COMMAND ${CMAKE_BINARY_DIR}/bin/OtsDaqDemo_t -o ${TEST_NAME} -p ${TEST_DBPROVIDER} -c ${TEST_COLLECTION_NAME} -f json -v ${TEST_VERSION_NAME}_${TEST_NUMBER}
  -g ${TEST_CONFIGURATION_NAME} -e OTSDAQ
  -s ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json -x ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json
         WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR})
set_tests_properties(OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t PROPERTIES
    ENVIRONMENT "ARTDAQ_DATABASE_URI=${ARTDAQ_DATABASE_URI_OTS_FILESYSTEM}")


SET ( TEST_DBPROVIDER "mongo" )
string(RANDOM LENGTH 5 ALPHABET 0123456789 RAND_NAME)
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json @ONLY )
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json @ONLY )
# Register test with CTest
add_test(NAME OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t
         COMMAND ${CMAKE_BINARY_DIR}/bin/OtsDaqDemo_t -o ${TEST_NAME} -p ${TEST_DBPROVIDER} -c ${TEST_COLLECTION_NAME} -f json -v ${TEST_VERSION_NAME}_${TEST_NUMBER}
  -g ${TEST_CONFIGURATION_NAME} -e OTSDAQ
  -s ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json -x ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json
         WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR})
set_tests_properties(OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t PROPERTIES
    ENVIRONMENT "ARTDAQ_DATABASE_URI=${ARTDAQ_DATABASE_URI_OTS_MONGO}")




SET (TEST_COLLECTION_NAME "FEConfiguration" )
SET ( TEST_FILE "${TEST_COLLECTION_NAME}_v0_pretty.json")

SET ( TEST_NUMBER "005" )

SET ( TEST_DBPROVIDER "filesystem" )
string(RANDOM LENGTH 5 ALPHABET 0123456789 RAND_NAME)
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json @ONLY )
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json @ONLY )
# Register test with CTest
add_test(NAME OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t
         COMMAND ${CMAKE_BINARY_DIR}/bin/OtsDaqDemo_t -o ${TEST_NAME} -p ${TEST_DBPROVIDER} -c ${TEST_COLLECTION_NAME} -f json -v ${TEST_VERSION_NAME}_${TEST_NUMBER}
  -g ${TEST_CONFIGURATION_NAME} -e OTSDAQ
  -s ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json -x ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json
         WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR})
set_tests_properties(OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t PROPERTIES
    ENVIRONMENT "ARTDAQ_DATABASE_URI=${ARTDAQ_DATABASE_URI_OTS_FILESYSTEM}")


SET ( TEST_DBPROVIDER "mongo" )
string(RANDOM LENGTH 5 ALPHABET 0123456789 RAND_NAME)
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json @ONLY )
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json @ONLY )
# Register test with CTest
add_test(NAME OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t
         COMMAND ${CMAKE_BINARY_DIR}/bin/OtsDaqDemo_t -o ${TEST_NAME} -p ${TEST_DBPROVIDER} -c ${TEST_COLLECTION_NAME} -f json -v ${TEST_VERSION_NAME}_${TEST_NUMBER}
  -g ${TEST_CONFIGURATION_NAME} -e OTSDAQ
  -s ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json -x ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json
         WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR})
set_tests_properties(OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t PROPERTIES
    ENVIRONMENT "ARTDAQ_DATABASE_URI=${ARTDAQ_DATABASE_URI_OTS_MONGO}")




SET (TEST_COLLECTION_NAME "FEWROtsUDPFSSRInterfaceConfiguration" )
SET ( TEST_FILE "${TEST_COLLECTION_NAME}_v0_pretty.json")

SET ( TEST_NUMBER "006" )

SET ( TEST_DBPROVIDER "filesystem" )
string(RANDOM LENGTH 5 ALPHABET 0123456789 RAND_NAME)
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json @ONLY )
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json @ONLY )
# Register test with CTest
add_test(NAME OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t
         COMMAND ${CMAKE_BINARY_DIR}/bin/OtsDaqDemo_t -o ${TEST_NAME} -p ${TEST_DBPROVIDER} -c ${TEST_COLLECTION_NAME} -f json -v ${TEST_VERSION_NAME}_${TEST_NUMBER}
  -g ${TEST_CONFIGURATION_NAME} -e OTSDAQ
  -s ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json -x ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json
         WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR})
set_tests_properties(OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t PROPERTIES
    ENVIRONMENT "ARTDAQ_DATABASE_URI=${ARTDAQ_DATABASE_URI_OTS_FILESYSTEM}")


SET ( TEST_DBPROVIDER "mongo" )
string(RANDOM LENGTH 5 ALPHABET 0123456789 RAND_NAME)
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json @ONLY )
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json @ONLY )
# Register test with CTest
add_test(NAME OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t
         COMMAND ${CMAKE_BINARY_DIR}/bin/OtsDaqDemo_t -o ${TEST_NAME} -p ${TEST_DBPROVIDER} -c ${TEST_COLLECTION_NAME} -f json -v ${TEST_VERSION_NAME}_${TEST_NUMBER}
  -g ${TEST_CONFIGURATION_NAME} -e OTSDAQ
  -s ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json -x ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json
         WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR})
set_tests_properties(OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t PROPERTIES
    ENVIRONMENT "ARTDAQ_DATABASE_URI=${ARTDAQ_DATABASE_URI_OTS_MONGO}")







SET (TEST_COLLECTION_NAME "FEWROtsUDPHCALInterfaceConfiguration" )
SET ( TEST_FILE "${TEST_COLLECTION_NAME}_v0_pretty.json")

SET ( TEST_NUMBER "007" )

SET ( TEST_DBPROVIDER "filesystem" )
string(RANDOM LENGTH 5 ALPHABET 0123456789 RAND_NAME)
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json @ONLY )
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json @ONLY )
# Register test with CTest
add_test(NAME OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t
         COMMAND ${CMAKE_BINARY_DIR}/bin/OtsDaqDemo_t -o ${TEST_NAME} -p ${TEST_DBPROVIDER} -c ${TEST_COLLECTION_NAME} -f json -v ${TEST_VERSION_NAME}_${TEST_NUMBER}
  -g ${TEST_CONFIGURATION_NAME} -e OTSDAQ
  -s ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json -x ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json
         WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR})
set_tests_properties(OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t PROPERTIES
    ENVIRONMENT "ARTDAQ_DATABASE_URI=${ARTDAQ_DATABASE_URI_OTS_FILESYSTEM}")


SET ( TEST_DBPROVIDER "mongo" )
string(RANDOM LENGTH 5 ALPHABET 0123456789 RAND_NAME)
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json @ONLY )
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json @ONLY )
# Register test with CTest
add_test(NAME OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t
         COMMAND ${CMAKE_BINARY_DIR}/bin/OtsDaqDemo_t -o ${TEST_NAME} -p ${TEST_DBPROVIDER} -c ${TEST_COLLECTION_NAME} -f json -v ${TEST_VERSION_NAME}_${TEST_NUMBER}
  -g ${TEST_CONFIGURATION_NAME} -e OTSDAQ
  -s ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json -x ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json
         WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR})
set_tests_properties(OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t PROPERTIES
    ENVIRONMENT "ARTDAQ_DATABASE_URI=${ARTDAQ_DATABASE_URI_OTS_MONGO}")







SET (TEST_COLLECTION_NAME "FEWRPurdueFSSRInterfaceConfiguration" )
SET ( TEST_FILE "${TEST_COLLECTION_NAME}_v0_pretty.json")

SET ( TEST_NUMBER "008" )

SET ( TEST_DBPROVIDER "filesystem" )
string(RANDOM LENGTH 5 ALPHABET 0123456789 RAND_NAME)
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json @ONLY )
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json @ONLY )
# Register test with CTest
add_test(NAME OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t
         COMMAND ${CMAKE_BINARY_DIR}/bin/OtsDaqDemo_t -o ${TEST_NAME} -p ${TEST_DBPROVIDER} -c ${TEST_COLLECTION_NAME} -f json -v ${TEST_VERSION_NAME}_${TEST_NUMBER}
  -g ${TEST_CONFIGURATION_NAME} -e OTSDAQ
  -s ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json -x ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json
         WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR})
set_tests_properties(OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t PROPERTIES
    ENVIRONMENT "ARTDAQ_DATABASE_URI=${ARTDAQ_DATABASE_URI_OTS_FILESYSTEM}")


SET ( TEST_DBPROVIDER "mongo" )
string(RANDOM LENGTH 5 ALPHABET 0123456789 RAND_NAME)
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json @ONLY )
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json @ONLY )
# Register test with CTest
add_test(NAME OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t
         COMMAND ${CMAKE_BINARY_DIR}/bin/OtsDaqDemo_t -o ${TEST_NAME} -p ${TEST_DBPROVIDER} -c ${TEST_COLLECTION_NAME} -f json -v ${TEST_VERSION_NAME}_${TEST_NUMBER}
  -g ${TEST_CONFIGURATION_NAME} -e OTSDAQ
  -s ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json -x ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json
         WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR})
set_tests_properties(OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t PROPERTIES
    ENVIRONMENT "ARTDAQ_DATABASE_URI=${ARTDAQ_DATABASE_URI_OTS_MONGO}")








SET (TEST_COLLECTION_NAME "FSSRDACsConfiguration" )
SET ( TEST_FILE "${TEST_COLLECTION_NAME}_v0_pretty.json")

SET ( TEST_NUMBER "009" )

SET ( TEST_DBPROVIDER "filesystem" )
string(RANDOM LENGTH 5 ALPHABET 0123456789 RAND_NAME)
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json @ONLY )
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json @ONLY )
# Register test with CTest
add_test(NAME OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t
         COMMAND ${CMAKE_BINARY_DIR}/bin/OtsDaqDemo_t -o ${TEST_NAME} -p ${TEST_DBPROVIDER} -c ${TEST_COLLECTION_NAME} -f json -v ${TEST_VERSION_NAME}_${TEST_NUMBER}
  -g ${TEST_CONFIGURATION_NAME} -e OTSDAQ
  -s ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json -x ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json
         WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR})
set_tests_properties(OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t PROPERTIES
    ENVIRONMENT "ARTDAQ_DATABASE_URI=${ARTDAQ_DATABASE_URI_OTS_FILESYSTEM}")


SET ( TEST_DBPROVIDER "mongo" )
string(RANDOM LENGTH 5 ALPHABET 0123456789 RAND_NAME)
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json @ONLY )
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json @ONLY )
# Register test with CTest
add_test(NAME OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t
         COMMAND ${CMAKE_BINARY_DIR}/bin/OtsDaqDemo_t -o ${TEST_NAME} -p ${TEST_DBPROVIDER} -c ${TEST_COLLECTION_NAME} -f json -v ${TEST_VERSION_NAME}_${TEST_NUMBER}
  -g ${TEST_CONFIGURATION_NAME} -e OTSDAQ
  -s ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json -x ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json
         WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR})
set_tests_properties(OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t PROPERTIES
    ENVIRONMENT "ARTDAQ_DATABASE_URI=${ARTDAQ_DATABASE_URI_OTS_MONGO}")





SET (TEST_COLLECTION_NAME "MaskConfiguration" )
SET ( TEST_FILE "${TEST_COLLECTION_NAME}_v0_pretty.json")

SET ( TEST_NUMBER "010" )

SET ( TEST_DBPROVIDER "filesystem" )
string(RANDOM LENGTH 5 ALPHABET 0123456789 RAND_NAME)
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json @ONLY )
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json @ONLY )
# Register test with CTest
add_test(NAME OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t
         COMMAND ${CMAKE_BINARY_DIR}/bin/OtsDaqDemo_t -o ${TEST_NAME} -p ${TEST_DBPROVIDER} -c ${TEST_COLLECTION_NAME} -f json -v ${TEST_VERSION_NAME}_${TEST_NUMBER}
  -g ${TEST_CONFIGURATION_NAME} -e OTSDAQ
  -s ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json -x ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json
         WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR})
set_tests_properties(OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t PROPERTIES
    ENVIRONMENT "ARTDAQ_DATABASE_URI=${ARTDAQ_DATABASE_URI_OTS_FILESYSTEM}")


SET ( TEST_DBPROVIDER "mongo" )
string(RANDOM LENGTH 5 ALPHABET 0123456789 RAND_NAME)
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json @ONLY )
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json @ONLY )
# Register test with CTest
add_test(NAME OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t
         COMMAND ${CMAKE_BINARY_DIR}/bin/OtsDaqDemo_t -o ${TEST_NAME} -p ${TEST_DBPROVIDER} -c ${TEST_COLLECTION_NAME} -f json -v ${TEST_VERSION_NAME}_${TEST_NUMBER}
  -g ${TEST_CONFIGURATION_NAME} -e OTSDAQ
  -s ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json -x ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json
         WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR})
set_tests_properties(OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t PROPERTIES
    ENVIRONMENT "ARTDAQ_DATABASE_URI=${ARTDAQ_DATABASE_URI_OTS_MONGO}")




SET (TEST_COLLECTION_NAME "ROCToFEConfiguration" )
SET ( TEST_FILE "${TEST_COLLECTION_NAME}_v0_pretty.json")

SET ( TEST_NUMBER "011" )

SET ( TEST_DBPROVIDER "filesystem" )
string(RANDOM LENGTH 5 ALPHABET 0123456789 RAND_NAME)
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json @ONLY )
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json @ONLY )
# Register test with CTest
add_test(NAME OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t
         COMMAND ${CMAKE_BINARY_DIR}/bin/OtsDaqDemo_t -o ${TEST_NAME} -p ${TEST_DBPROVIDER} -c ${TEST_COLLECTION_NAME} -f json -v ${TEST_VERSION_NAME}_${TEST_NUMBER}
  -g ${TEST_CONFIGURATION_NAME} -e OTSDAQ
  -s ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json -x ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json
         WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR})
set_tests_properties(OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t PROPERTIES
    ENVIRONMENT "ARTDAQ_DATABASE_URI=${ARTDAQ_DATABASE_URI_OTS_FILESYSTEM}")


SET ( TEST_DBPROVIDER "mongo" )
string(RANDOM LENGTH 5 ALPHABET 0123456789 RAND_NAME)
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json @ONLY )
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json @ONLY )
# Register test with CTest
add_test(NAME OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t
         COMMAND ${CMAKE_BINARY_DIR}/bin/OtsDaqDemo_t -o ${TEST_NAME} -p ${TEST_DBPROVIDER} -c ${TEST_COLLECTION_NAME} -f json -v ${TEST_VERSION_NAME}_${TEST_NUMBER}
  -g ${TEST_CONFIGURATION_NAME} -e OTSDAQ
  -s ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json -x ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json
         WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR})
set_tests_properties(OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t PROPERTIES
    ENVIRONMENT "ARTDAQ_DATABASE_URI=${ARTDAQ_DATABASE_URI_OTS_MONGO}")




SET (TEST_COLLECTION_NAME "VersionAliases" )
SET ( TEST_FILE "${TEST_COLLECTION_NAME}_v0_pretty.json")

SET ( TEST_NUMBER "012" )

SET ( TEST_DBPROVIDER "filesystem" )
string(RANDOM LENGTH 5 ALPHABET 0123456789 RAND_NAME)
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json @ONLY )
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json @ONLY )
# Register test with CTest
add_test(NAME OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t
         COMMAND ${CMAKE_BINARY_DIR}/bin/OtsDaqDemo_t -o ${TEST_NAME} -p ${TEST_DBPROVIDER} -c ${TEST_COLLECTION_NAME} -f json -v ${TEST_VERSION_NAME}_${TEST_NUMBER}
  -g ${TEST_CONFIGURATION_NAME} -e OTSDAQ
  -s ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json -x ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json
         WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR})
set_tests_properties(OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t PROPERTIES
    ENVIRONMENT "ARTDAQ_DATABASE_URI=${ARTDAQ_DATABASE_URI_OTS_FILESYSTEM}")


SET ( TEST_DBPROVIDER "mongo" )
string(RANDOM LENGTH 5 ALPHABET 0123456789 RAND_NAME)
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json @ONLY )
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/${TEST_FILE} ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json @ONLY )
# Register test with CTest
add_test(NAME OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t
         COMMAND ${CMAKE_BINARY_DIR}/bin/OtsDaqDemo_t -o ${TEST_NAME} -p ${TEST_DBPROVIDER} -c ${TEST_COLLECTION_NAME} -f json -v ${TEST_VERSION_NAME}_${TEST_NUMBER}
  -g ${TEST_CONFIGURATION_NAME} -e OTSDAQ
  -s ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.src.${RAND_NAME}.json -x ${CMAKE_CURRENT_BINARY_DIR}/${TEST_FILE}.cmp.${RAND_NAME}.json
         WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR})
set_tests_properties(OtsDaqDemo_${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_t PROPERTIES
    ENVIRONMENT "ARTDAQ_DATABASE_URI=${ARTDAQ_DATABASE_URI_OTS_MONGO}")
