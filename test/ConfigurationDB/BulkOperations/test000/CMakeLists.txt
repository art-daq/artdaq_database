SET ( TEST_NAME "BulkOperations_seeddata" )

SET ( TEST_NUMBER "000" )

##### newconfig
SET ( TEST_COLLECTION_NAME_QUAL "newconfig" )

SET ( TEST_FILE_NUMBER "013" )

SET ( TEST_DBPROVIDER "filesystem" )
string(RANDOM LENGTH 5 ALPHABET 0123456789 RAND_NAME)
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/test${TEST_FILE_NUMBER}.src.json ${CMAKE_CURRENT_BINARY_DIR}/test${TEST_FILE_NUMBER}.src.${RAND_NAME}.json @ONLY )
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/test${TEST_FILE_NUMBER}.cmp.json ${CMAKE_CURRENT_BINARY_DIR}/test${TEST_FILE_NUMBER}.cmp.${RAND_NAME}.json @ONLY )
# Register test with CTest
add_test(NAME ${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_${TEST_FILE_NUMBER}_t
         COMMAND ${CMAKE_BINARY_DIR}/bin/ConfigurationDB_t -o store -p ${TEST_DBPROVIDER} -c ${TEST_COLLECTION_NAME}_${TEST_COLLECTION_NAME_QUAL}_01 -f json -v ${TEST_VERSION_NAME} -g ${TEST_COLLECTION_NAME_QUAL}-${TEST_CONFIGURATION_NAME}-A -e ${TEST_COLLECTION_NAME_QUAL}-${CONFIG_NUMBER}-A -s test${TEST_FILE_NUMBER}.src.${RAND_NAME}.json -x test${TEST_FILE_NUMBER}.cmp.${RAND_NAME}.json
         WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR})
set_tests_properties(${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_${TEST_FILE_NUMBER}_t PROPERTIES
    ENVIRONMENT "ARTDAQ_DATABASE_URI=${ARTDAQ_DATABASE_URI_FILESYSTEM}")

SET ( TEST_DBPROVIDER "mongo" )
string(RANDOM LENGTH 5 ALPHABET 0123456789 RAND_NAME)
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/test${TEST_FILE_NUMBER}.src.json ${CMAKE_CURRENT_BINARY_DIR}/test${TEST_FILE_NUMBER}.src.${RAND_NAME}.json @ONLY )
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/test${TEST_FILE_NUMBER}.cmp.json ${CMAKE_CURRENT_BINARY_DIR}/test${TEST_FILE_NUMBER}.cmp.${RAND_NAME}.json @ONLY )
# Register test with CTest
add_test(NAME ${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_${TEST_FILE_NUMBER}_t
         COMMAND ${CMAKE_BINARY_DIR}/bin/ConfigurationDB_t -o store -p ${TEST_DBPROVIDER} -c ${TEST_COLLECTION_NAME}_${TEST_COLLECTION_NAME_QUAL}_01 -f json -v ${TEST_VERSION_NAME} -g ${TEST_COLLECTION_NAME_QUAL}-${TEST_CONFIGURATION_NAME}-A -e ${TEST_COLLECTION_NAME_QUAL}-${CONFIG_NUMBER}-A -s test${TEST_FILE_NUMBER}.src.${RAND_NAME}.json -x test${TEST_FILE_NUMBER}.cmp.${RAND_NAME}.json
         WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR})
set_tests_properties(${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_${TEST_FILE_NUMBER}_t PROPERTIES
    ENVIRONMENT "ARTDAQ_DATABASE_URI=${ARTDAQ_DATABASE_URI_MONGO}")

SET ( TEST_FILE_NUMBER "014" )

SET ( TEST_DBPROVIDER "filesystem" )
string(RANDOM LENGTH 5 ALPHABET 0123456789 RAND_NAME)
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/test${TEST_FILE_NUMBER}.src.json ${CMAKE_CURRENT_BINARY_DIR}/test${TEST_FILE_NUMBER}.src.${RAND_NAME}.json @ONLY )
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/test${TEST_FILE_NUMBER}.cmp.json ${CMAKE_CURRENT_BINARY_DIR}/test${TEST_FILE_NUMBER}.cmp.${RAND_NAME}.json @ONLY )
# Register test with CTest
add_test(NAME ${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_${TEST_FILE_NUMBER}_t
         COMMAND ${CMAKE_BINARY_DIR}/bin/ConfigurationDB_t -o store -p ${TEST_DBPROVIDER} -c ${TEST_COLLECTION_NAME}_${TEST_COLLECTION_NAME_QUAL}_02 -f json -v ${TEST_VERSION_NAME} -g ${TEST_COLLECTION_NAME_QUAL}-${TEST_CONFIGURATION_NAME}-B -e ${TEST_COLLECTION_NAME_QUAL}-${CONFIG_NUMBER}-B -s test${TEST_FILE_NUMBER}.src.${RAND_NAME}.json -x test${TEST_FILE_NUMBER}.cmp.${RAND_NAME}.json
         WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR})
set_tests_properties(${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_${TEST_FILE_NUMBER}_t PROPERTIES
    ENVIRONMENT "ARTDAQ_DATABASE_URI=${ARTDAQ_DATABASE_URI_FILESYSTEM}")

SET ( TEST_DBPROVIDER "mongo" )
string(RANDOM LENGTH 5 ALPHABET 0123456789 RAND_NAME)
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/test${TEST_FILE_NUMBER}.src.json ${CMAKE_CURRENT_BINARY_DIR}/test${TEST_FILE_NUMBER}.src.${RAND_NAME}.json @ONLY )
configure_file ( ${CMAKE_CURRENT_SOURCE_DIR}/test${TEST_FILE_NUMBER}.cmp.json ${CMAKE_CURRENT_BINARY_DIR}/test${TEST_FILE_NUMBER}.cmp.${RAND_NAME}.json @ONLY )
# Register test with CTest
add_test(NAME ${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_${TEST_FILE_NUMBER}_t
         COMMAND ${CMAKE_BINARY_DIR}/bin/ConfigurationDB_t -o store -p ${TEST_DBPROVIDER} -c ${TEST_COLLECTION_NAME}_${TEST_COLLECTION_NAME_QUAL}_02 -f json -v ${TEST_VERSION_NAME} -g ${TEST_COLLECTION_NAME_QUAL}-${TEST_CONFIGURATION_NAME}-B -e ${TEST_COLLECTION_NAME_QUAL}-${CONFIG_NUMBER}-B -s test${TEST_FILE_NUMBER}.src.${RAND_NAME}.json -x test${TEST_FILE_NUMBER}.cmp.${RAND_NAME}.json
         WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR})
set_tests_properties(${TEST_NAME}_${TEST_DBPROVIDER}_${TEST_NUMBER}_${TEST_FILE_NUMBER}_t PROPERTIES
    ENVIRONMENT "ARTDAQ_DATABASE_URI=${ARTDAQ_DATABASE_URI_MONGO}")
