# Check if mongod binary was found and conditionally start MongoDB
# Use shell scripts so CTest can execute complex shell logic
set(CTEST_CUSTOM_PRE_TEST "${CMAKE_BINARY_DIR}/ctest_pre_test.sh")
set(CTEST_CUSTOM_POST_TEST "${CMAKE_BINARY_DIR}/ctest_post_test.sh")
